export interface News {
  description: string;
  date: Date;
  id: string;
  link: string;
}

export interface Music {
  id: string;
  title: string;
}

export interface Art {
  id: string;
  title: string;
  url: string;
}
